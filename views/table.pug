extends layout

block content
    table
        tr
            th= name
        if all_rows.length > 0
            each row in all_rows
                tr
                    td= JSON.stringify(row)
        else
            tr.error: td Empty
    p
        a(href="..") < Back to Tables
    
